# Usage

Use CLI for setup, lifecycle, and policy management.
Use `./reco restart` after config changes.
Use IM commands `/new`, `/ask`, `/cwd`, `/interrupt`, `/approve`, `/status` once daemon is running.
