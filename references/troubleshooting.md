# Troubleshooting

- `doctor` fails for Codex: run `codex --version` and `codex login`.
- No IM responses: validate tokens and allowlists in config.
- Approval timeout: re-run command and approve within timeout window.
